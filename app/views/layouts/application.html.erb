<!DOCTYPE html>
<html>
  <head>
    <title>Excel-lent palettes: Color Schemes from Your Nightmares</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <meta name="turbo-refresh-method" content="morph">
  </head>

  <body>

  <header class="p-5 text-center border-b-4 border-green-300">
    <a href="/" class="text-4xl font-bold underline decoration-pink-500 decoration-wavy underline-offset-8 mb-2" style="font-family: 'Press Start 2P', cursive;">
      Excel-lent Palettes
    </a>
    <p class="text-lg font-light italic text-amber-500 animate-[header-animation_1s_infinite]">
      Color Schemes from Your Nightmares
    </p>

    <div class="flex justify-center space-x-4 mt-3">
      <% if current_user.present? %>
        <%= link_to "New Palette", new_palette_path, class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded shadow transform rotate-3 rainbow-hover" %>
        <%= button_to "Sign Out", sign_out_users_path, method: "delete", class: "bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded shadow transform -rotate-6 glitch" %>
      <% else %>
        <%= link_to "Sign In", sign_in_users_path, class: "bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded shadow transform -rotate-9 glitch" %>
      <% end %>
    </div>
    </header>

    <main class="container mx-auto mt-5 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
